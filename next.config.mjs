/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['th.bing.com'], // Add your image domains here if you're using external URLs
      },
};

export default nextConfig;
